<?php session_start();
use Application\Model\Gruppenmitglied;?>
<?=$msg?>
<?=$err?>


<table>

	<tr>
		<th>Gruppenbild</th>
		<th>GruppenNr</th>
		<th>Gruppenname</th>
		<th>Beschreibung
		<th></th>
	</tr>
	
	<?php 
	foreach ($gruppenListe as $zaehler => $gruppe) {
		
	?>

		<tr>
			<td><img src =<?=$gruppe->getGruppenbildpfad()?> height = "50" width = "50" alt = "Kein Bild vorhanden"></td>
			<td><?=$gruppe->getG_id()?></td>
			<td><?=$gruppe->getGruppenname()?></td>
			<td><?=$gruppe->getGruppenbeschreibung()?></td>
			<td>
			<td><a href='groupshow?g_id=<?=$gruppe->getG_id()?>'><img src="img/show.jpg" /></a></td>
			<?php //Nicht schön programmiert
			foreach ($gruppenadminListe as $counter => $gruppenmitglied) {
			if ($gruppenmitglied->getG_id()==$gruppe->getG_id() && $gruppenmitglied->getU_id()==$u_id && $gruppenmitglied->getGruppenadmin()==1) { ?>
			<td><a href='groupedit?g_id=<?=$gruppe->getG_id()?>'><img src="img/edit.jpg" /></a></td>
			<td><a href='groupdelete?g_id=<?=$gruppe->getG_id()?>'><img src="img/delete.png" /></a></td>
			<?php }
			else { ?>
				<td></td>
				<td></td>
			<?php }?>
			
		</tr>

	<?php 
		}
	}
	?>
		<tr><td> </td></tr>
		<tr>
		<th><a href='gruppeanlegen'>Neue Gruppe anlegen</a></th>
		</tr>

</table>	
	
	
	
	
	
	
	
<!-- Alternative, falls es spinnt (und Anzahl in Controller hochz�hlen):
	<?php 
	
	//for ($zaehler=0; $zaehler<$anzahl; $zaehler++) {
	?>

		<tr>
			<td><? //$gruppenListe[$zaehler]->getG_id()?></td>
			<td><? //$gruppenListe[$zaehler]->getGruppenname()?></td>
		</tr>

	<?php 
	//}
	?> -->

